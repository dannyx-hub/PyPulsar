# PyPulsar Basic Template – dependencies
# Run: pip install -r requirements.txt
pypulsar
pywebview>=5.1          # Core – native webview windows
typer[all]>=0.12         # Beautiful CLI (pypulsar dev/build)